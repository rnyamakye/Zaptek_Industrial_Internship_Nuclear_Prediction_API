$kind: http-request
url: '{{base_url}}/predictions/{{prediction_id}}'
method: DELETE
headers:
  Authorization: Bearer {{access_token}}
scripts:
  - type: afterResponse
    code: pm.test("Delete returns 204", function () { pm.response.to.have.status(204); });
    language: text/javascript
order: 5000
